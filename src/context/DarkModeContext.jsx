import { createContext } from "react"

const DarkModeContext = createContext()

const DarkModeContextComponent = () => {
  return (
    <div>DarkModeContext</div>
  )
}

export default DarkModeContextComponent